<?xml version="1.0" encoding="utf-8"?>
<!--
    The icon is a full bleed pattern, so all of it is the background layer: that layer is
    the one a launcher leaves alone, while the foreground is the one it may shift for its
    parallax. This is here because an adaptive icon declares both.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
